<% @users.each do |user| %>
  <p><%= link_to "#{user.id} : #{user.username}", profile_path(user.id) %></p>
<% end %>

<p class="pagenation_count"><%= page_entries_info @users %></p>

<div class="pagenation_bar">
  <%= paginate @users %>
</div>